/**
 * Onboarding module: orchestrates tenant signup across the tenant and identity modules (avoiding a
 * dependency cycle between them) and will own the vertical-preset application (modules,
 * terminology, seed roles) as it grows.
 */
package com.omnia.platform.onboarding;
