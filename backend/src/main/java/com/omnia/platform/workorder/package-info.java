/**
 * Work order module: longer-running service jobs (oficinas/assistências) with items, status flow and
 * approval. References the customer by weak id + snapshot (DOMAIN.md §3).
 */
package com.omnia.platform.workorder;
