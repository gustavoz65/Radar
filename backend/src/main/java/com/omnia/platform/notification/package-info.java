/**
 * Notification module: in-app notification center and transactional e-mail. Reacts to other
 * modules' domain events (e.g. {@code AppointmentBooked}) without them knowing it exists (ADR-006),
 * and exposes an e-mail port for direct sends.
 */
package com.omnia.platform.notification;
