/**
 * The fixtures do not redeclare the institutions: they come from the shared
 * catalogue in `lib/shared/institutions.ts`, the same one the Pierre mapper uses.
 */
export { institutionById, institutions } from '@/lib/shared/institutions';
